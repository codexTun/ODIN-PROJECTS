<h1>All Tags</h1>

<ul id = "articles">
	<% @tags.each do |tag| %>
		<li>
			<%= link_to tag.name, tag_path(tag) %>
		</li>
	<% end %>
</ul>
<%= link_to "Create a new Tag", new_tag_path, class: "new_article" %>



